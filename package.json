{
  "name": "portfolio-2024",
  "description": "Paul's portfolio",
  "scripts": {
    "start": "npx @11ty/eleventy --watch --serve --quiet",
    "build": "npx @11ty/eleventy",
    "lint:check": "prettier --check .",
    "lint": "prettier --write ."
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "@shopify/prettier-plugin-liquid": "^1.5.2",
    "clean-css": "^5.3.3",
    "prettier": "^3.3.3"
  },
  "type": "module",
  "dependencies": {
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2"
  }
}
